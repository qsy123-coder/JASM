请帮我为我的 C# Mod 管理器编写核心内容区域的“Mod 卡片网格（Mod Grid Cards）”界面布局代码。



【核心网格容器要求】
- 外层使用 ScrollViewer 包裹。
- 核心区域使用支持自适应换行的容器（如 WrapPanel 或结合 ItemsControl），每个卡片单元项（Item）的固定宽度设为 210px，间距（Margin）为 8px，确保随窗口拉伸自动流式排版。

【单张卡片（DataTemplate / UserControl）详细布局指南】
卡片为纯白背景（#FFFFFF），带微小圆角（BorderRadius="8"）和 1px 浅灰色外边框。内部从上到下垂直堆叠：

1. 顶部预览图（Image）：
   - 高度固定（约 120px），Width="Stretch"，图片拉伸方式为 UniformToFill，且上方两角裁剪圆角。
   - 实现一个绝对定位的右上角 Overlay 容器：当 Mod 属性为 NSFW 时，右上角显示一个红底白字（背景色 #FF4D4F，圆角胶囊状）的 "NSFW" 标签。
   - 若无图片，则居中显示一个默认的图片占位图标（可用字符 🖼️ 或 Path 代替）。

2. Mod 标题（TextBlock）：
   - Margin="8,8,8,4"，黑色字体，加粗。
   - 限制最大行数为 2 行，超出部分自动显示省略号（Trimming/Ellipsis）。

3. 作者与时间行（StackPanel Horizontal）：
   - Margin="8,4,8,4"，整体使用浅灰色字体（如 #666666），字号 11px。
   - 包含：[圆形小头像 Border/Image 16x16] + [作者名字] + [间距] + [时间字符串，如 "9小时前"]。

4. 底部数据状态行（StackPanel Horizontal）：
   - Margin="8,4,8,8"，小字号，灰色图标。
   - 并排展示三个数据项（图标与数字靠左紧凑排列，项与项之间留有间距）：
     * 项 1：💙 {点赞数}
     * 项 2：👁️ {浏览数}
     * 项 3：💬 {评论数}

【代码质量要求】
- 请提供标准的 ItemsControl 及其 ItemTemplate 代码。
- 确保整个卡片在高度上是固定的（或由内容撑开但整齐划一），不会因为标题长短不一而导致卡片错位。
- 预留好各字段（图片路径、标题、作者名、时间、三组数据、是否显示 NSFW）的数据绑定（Binding）接口。