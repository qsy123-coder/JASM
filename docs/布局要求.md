【视觉风格与布局还原指南】
请完美还原以下视觉布局与设计规范：

1. 整体色调：
   - 采用现代扁平化、高雅的浅色模式（Light Mode）。
   - 背景色为极其柔和的浅灰白（如 #F5F7FA 或 #F8F9FA），卡片背景为纯白（#FFFFFF），带有微弱的圆角（如 CornerRadius="8"）和极轻的阴影。

2. 布局结构：
   - 左侧分类栏 (Width="240")：
     * 顶部为“分类”标题文本（字号较大，加粗）。
     * 下方是一个垂直滚动列表（ListBox/ScrollViewer），每个 Item 包含：[左侧 16x16 像素的 Icon/圆形头像] + [分类名称文本] + [靠右对齐的灰色数量文本（如 2666）]。
   - 右侧主面板 (填充剩余空间)：
     * 顶部工具栏 (Height="50")：
       - 一个圆角搜索输入框，左侧有放大镜占位图标，提示文字为“搜索模组...”。
       - 右侧有三个并排的下拉选择框（ComboBox）："类型筛选"、"内容过滤"、"排序规则（最新/最热）"。
       - 最右侧是一个独立的下载任务管理图标按钮。
     * 核心内容区 (Grid View)：
       - 使用 WrapPanel 或 UniformGrid 实现自适应网格，单个卡片宽度约为 200px~240px。
       - 卡片内部采用垂直堆叠布局：
         1) [预览图区]：高宽比约为 3:4，支持圆角裁剪。图片右上角支持绝对定位一个红底白字（#FF4D4F）的“NSFW”小标签（胶囊形状）。
         2) [标题文本]：紧跟在图片下方，黑字，加粗，14px，支持两行省略。
         3) [作者信息行]：[小圆形头像] + [作者名字（灰色）] + [发布时间（右对齐或紧跟其后，如：1小时前）]，字号 11px。
         4) [统计数据行]：三个并列的灰色小图标与数值。格式为：
            - 💙 {Likes}  |  📥 {Downloads}  |  💬 {Comments}