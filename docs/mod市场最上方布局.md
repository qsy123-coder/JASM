请帮我为我的 C# Mod 管理器编写顶部“工具筛选栏（Toolbar）”的界面布局代码。



【详细布局与内容还原要求】
请在一个水平排列的容器（如 Grid 或 StackPanel）中，从左到右实现以下组件布局：

1. 弹性搜索输入框（TextBox）：
   - 宽度自适应（Width="*"），占满左侧剩余所有空间。
   - 圆角矩形，灰色边框，内衬提示词为“搜索模组...”。
   - 靠右侧内部放置一个线型放大镜图标（🔍）。

2. 筛选下拉框组合（右侧对齐，水平排列，组件间距 Margin="0,0,8,0"）：
   - 统一视觉：高度一致，纯白背景，圆角边框，右侧带向下箭头（ˇ）。
   
   - 下拉框 1（分类筛选）：
     * 默认选中值："仅Mods"
     * 数据源列表（Items）：["仅Mods", "全部分类"]
     
   - 下拉框 2（内容安全过滤）：
     * 默认选中值："显示 NSFW"
     * 数据源列表（Items）：["显示 NSFW", "模糊 NSFW", "隐藏 NSFW"]
     
   - 下拉框 3（排序规则）：
     * 默认选中值："最新"
     * 数据源列表（Items）：["最新", "最近更新", "默认"]

3. 最右侧下载按钮（Button）：
   - 紧跟在第三个下拉框右侧。
   - 正方形无边框或浅圆角按钮，与下拉框等高。
   - 中心图标为标准的线型下载管理图标（📥）。

【代码质量要求】
- 如果使用 XAML，请合理使用 Grid 的 ColumnDefinition 将第一列设为 `*`，后续列设为 `Auto`，以确保搜索框的弹性响应式缩放。
- 请直接在 ComboBox 中硬编码或绑定好我提供的这几组具体选项内容，方便我直接复制使用。
- 保持各组件垂直居中对齐，留白呼吸感紧凑现代。